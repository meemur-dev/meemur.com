{
  "name": "meemur-com",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "meemur.com — coming soon page",
  "scripts": {
    "dev": "wrangler pages dev ./public",
    "predev": "node scripts/build-version.mjs",
    "deploy": "wrangler pages deploy ./public --project-name meemur-com",
    "stamp": "node scripts/build-version.mjs",
    "pretest": "node scripts/build-version.mjs",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "wrangler": "^4.96.0"
  }
}
